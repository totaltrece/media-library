{
  "name": "@media-library/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc",
    "start": "node dist/main.js",
    "test": "pnpm --filter @media-library/indexer build && pnpm --filter @media-library/search build && tsc --project tsconfig.test.json && node --test dist-test/test/health.test.js dist-test/test/search.test.js"
  },
  "dependencies": {
    "@media-library/indexer": "workspace:*",
    "@media-library/search": "workspace:*",
    "fastify": "^5.7.4"
  }
}
{
  "name": "@media-library/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc",
    "start": "node dist/main.js",
    "test": "tsc --project tsconfig.test.json && node --test dist-test/test/health.test.js"
  },
  "dependencies": {
    "fastify": "^5.7.4"
  }
}